<%= form_with(model: sale, local: true) do |form| %>
  <% if sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sale.errors.count, "error") %> prohibited this sale from being saved:</h2>

      <ul>
      <% sale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.select :customer_id, @customer_choice %>
  </div>

  <div class="field">
    <%= form.label :saledate %>
    <%= form.date_select :saledate, id: :sale_saledate %>
  </div>

  <div class="field">
    <%= form.label :salenotes %>
    <%= form.text_area :salenotes, id: :sale_salenotes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
